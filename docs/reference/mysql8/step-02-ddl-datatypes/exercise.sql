-- =====================================================================
-- Step 02 exercise.sql — 연습문제
-- 실습 테이블은 반드시 s02_ 접두사를 쓰세요. 정답은 solution.sql
-- =====================================================================
USE shop;

-- 문제 1. s02_coupons 테이블을 설계하시오.
--   요구사항:
--     - 쿠폰 ID (자동 증가, 앞으로 수억 장 발행 예정)
--     - 쿠폰 코드 (16자 고정, 중복 불가)
--     - 할인 방식: 'RATE'(정률) 또는 'AMOUNT'(정액)
--     - 할인율 (0~100, 소수점 둘째 자리까지) 또는 할인금액 (원 단위, 정확해야 함)
--     - 최소 주문 금액
--     - 유효 시작일시 / 종료일시
--     - 사용 여부 (참/거짓)
--     - 생성 시각(자동), 수정 시각(자동 갱신)
--   힌트: 돈은 무슨 타입? ID는 무슨 타입? 참/거짓은?


-- 문제 2. shop DB에서 NULL 을 허용하는 컬럼을 모두 찾으시오.
--   (테이블명, 컬럼명, 타입을 함께 출력. information_schema 사용)


-- 문제 3. shop DB의 DECIMAL 타입 컬럼을 모두 나열하시오.
--   (테이블명, 컬럼명, 전체 자릿수, 소수 자릿수)


-- 문제 4. s02_coupons 에 'used_count INT NOT NULL DEFAULT 0' 컬럼을 추가하되,
--   온라인 DDL(테이블 복사 없이)이 불가능하면 아예 실패하도록 작성하시오.


-- 문제 5. TIMESTAMP 컬럼에 '2039-01-01' 을 넣으면 어떻게 되는가? 직접 확인하시오.
--   왜 그런지 설명할 수 있어야 합니다.


-- 문제 6. CHAR(3) 컬럼에 4글자를 넣으면?
--   (a) 엄격 모드(기본)에서
--   (b) SET SESSION sql_mode='' 로 끄고 나서
--   두 경우가 어떻게 다른지 확인하고, 왜 엄격 모드를 끄면 안 되는지 설명하시오.


-- 문제 7. orders 테이블의 status ENUM 에 'REFUNDED' 를 추가하려면 어떤 문장이 필요한가?
--   (실제로 실행하지는 마세요! orders 는 공용 테이블입니다.
--    s02_orders_copy 사본을 만들어 거기서 실습하세요.)


-- 정리
-- DROP TABLE IF EXISTS s02_coupons, s02_orders_copy;
