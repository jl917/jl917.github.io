-- =====================================================================
-- Step 04 — SELECT 기본 : exercise.sql (문제)
-- 정답은 solution.sql 을 보기 전에 스스로 먼저 작성해 보세요.
-- 실행: mysql -h127.0.0.1 -P3307 -ulearner -plearn1234 shop -t < exercise.sql
-- =====================================================================
USE shop;

-- ---------------------------------------------------------------------
-- [문제 1] 품절 상품 찾기
--   products 에서 status 가 'SOLD_OUT' 인 상품의
--   product_id, name, stock 을 조회하세요.
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- [문제 2] 포인트 상위 3명 (한글 별칭)
--   customers 에서 points 가 높은 순으로 상위 3명의 name, grade, points 를
--   조회하되, 컬럼 별칭을 각각 이름 / 등급 / 포인트 로 붙이세요.
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- [문제 3] 배송 도시 목록
--   orders 의 shipping_city 를 중복 없이 조회하세요.
--   (몇 개가 나오나요? customers.city 의 DISTINCT 결과와 비교해 보세요.)
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- [문제 4] 고가 + 재고부족 상품
--   products 에서 가격이 100,000 이상이고 재고가 30 미만인 상품을
--   가격 내림차순으로 조회하세요. (product_id, name, price, stock)
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- [문제 5] 연봉 4위 사원
--   employees 에서 salary 가 4번째로 높은 사원 1명을 조회하세요.
--   힌트: ORDER BY + LIMIT ... OFFSET ...
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- [문제 6] 재고 자산가치 TOP 5
--   products 에서 product_id, name, price * stock 을 조회하세요.
--   - price * stock 의 별칭은 stock_value
--   - stock_value 내림차순 상위 5개
--   - 동점이면 product_id 오름차순으로 tie-break
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- [문제 7] NULL 을 뒤로 보내는 정렬
--   customers 에서 phone 이 NULL 인 고객을 맨 뒤로 보내면서,
--   나머지는 created_at 최신순으로 정렬해 상위 6명을 조회하세요.
--   (customer_id, name, phone, created_at)
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- [문제 8] 에러 고치기 (생각해보기)
--   아래 쿼리는 왜 에러가 날까요? 두 가지 방법으로 고쳐 보세요.
--
--     SELECT customer_id, name, points * 2 AS bonus
--     FROM customers
--     WHERE bonus > 20000;
--
--   고친 쿼리 ① (식 반복):
--
--   고친 쿼리 ② (파생 테이블):
-- ---------------------------------------------------------------------
