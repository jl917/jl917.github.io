-- =====================================================================
-- Step 10 — 집합 연산 : 연습문제
-- 실행: mysql -h127.0.0.1 -P3307 -ulearner -plearn1234 shop < exercise.sql
-- 주의: INTERSECT / EXCEPT 는 MySQL 8.0.31 이상 필요
-- 정답은 solution.sql
-- =====================================================================
USE shop;

-- ---------------------------------------------------------------------
-- Q1. VIP 고객이 사는 도시와 GOLD 고객이 사는 도시를
--     중복 없이 하나의 목록으로 출력하시오. (컬럼: city / 정렬: city)
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q2. Q1 을 UNION ALL 로 바꾸어 행 수를 세시오.
--     UNION 결과 행 수와 왜 다른지 주석으로 설명할 것.
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q3. 카테고리 21(노트북)과 23(주변기기)에서
--     각각 "최고가 상품 1개씩"만 뽑아 하나의 결과로 출력하시오.
--     컬럼: category_id, name, price  (총 2행)
--     힌트: 괄호 + ORDER BY + LIMIT
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q4. INTERSECT 로
--     "후기가 달린 상품" ∩ "재고(stock)가 100개 이상인 상품" 을 구하시오.
--     컬럼: product_id / 정렬: product_id
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q5. EXCEPT 로
--     "주문된 적 있는 상품(order_items)" - "후기가 달린 상품(reviews)" 을 구하시오.
--     컬럼: product_id / 정렬: product_id
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q6. Q5 와 같은 결과를 NOT EXISTS 로 구하고,
--     두 방법의 결과 건수가 같은지 확인하시오.
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q7. orders 와 payments 를 FULL OUTER JOIN 처럼 합치시오.
--     (결제 없는 주문 + 주문 없는 결제 + 매칭된 것 모두)
--     컬럼: order_id, order_status, payment_id, pay_status
--     정렬: order_id / 상위 5행
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q8. 등급(grade)별 고객 수를 구하고, 맨 아래에 '__TOTAL__' 합계 행을 붙이시오.
--     컬럼: grade, cnt
-- ---------------------------------------------------------------------
