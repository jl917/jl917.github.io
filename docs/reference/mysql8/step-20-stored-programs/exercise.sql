-- =====================================================================
-- Step 20 — 저장 프로그램  exercise.sql
-- ---------------------------------------------------------------------
-- 먼저 practice.sql 을 실행해 s20_ 사본과 객체가 있는 상태에서 푸세요.
-- (또는 solution.sql 첫 부분처럼 사본을 다시 만드세요)
-- 모든 객체 이름에 s20ex_ 접두사를 쓰세요. 공용 테이블 변경 금지.
-- mysql 클라이언트로 실행하세요 (DELIMITER 사용).
-- =====================================================================
USE shop;

-- ---------------------------------------------------------------------
-- [문제 1] 할인가 계산 함수
--   s20ex_discounted(price, pct) : price 에서 pct(%) 할인한 가격을 반환.
--   DETERMINISTIC 을 선언하세요. 결과는 정수로 반올림.
--   예) s20ex_discounted(10000, 15) = 8500
-- ---------------------------------------------------------------------
-- 여기에 작성


-- ---------------------------------------------------------------------
-- [문제 2] 등급별 배송비 함수 (CASE)
--   s20ex_shipping_fee(grade) :
--     VIP → 0, GOLD → 1500, 그 외 → 3000 을 반환.
--   grade 는 문자열('VIP','GOLD','SILVER','BRONZE').
-- ---------------------------------------------------------------------
-- 여기에 작성


-- ---------------------------------------------------------------------
-- [문제 3] 재입고 프로시저
--   s20ex_restock(IN p_id INT, IN p_qty INT, OUT p_new_stock INT) :
--     s20_products 의 재고를 p_qty 만큼 늘리고, 새 재고를 OUT 으로 반환.
--     s20_audit 에 'RESTOCK' 로그도 남기세요.
--     (p_qty <= 0 이면 SIGNAL 로 에러)
-- ---------------------------------------------------------------------
-- 여기에 작성


-- ---------------------------------------------------------------------
-- [문제 4] 커서로 저재고 목록
--   s20ex_low_stock(IN p_threshold INT) :
--     재고가 p_threshold 미만인 상품을 커서로 훑어
--     'id:name(stock)' 를 콤마로 이어 하나의 문자열로 반환(SELECT).
--   (커서 + CONTINUE HANDLER FOR NOT FOUND 사용)
-- ---------------------------------------------------------------------
-- 여기에 작성


-- ---------------------------------------------------------------------
-- [문제 5] 주문 취소 트리거 (AFTER DELETE)
--   s20_order_items 에서 행이 삭제되면, 그 수량만큼 s20_products.stock 을
--   되돌리고 s20_audit 에 'CANCEL' 로그를 남기는 트리거를 만드세요.
--   트리거 이름은 s20ex_ad_order_items.
--   만든 뒤 실제로 한 행을 INSERT 했다가 DELETE 해서 재고가 복원되는지 확인.
-- ---------------------------------------------------------------------
-- 여기에 작성


-- ---------------------------------------------------------------------
-- [문제 6] 입력 검증 프로시저
--   s20ex_set_price(IN p_id INT, IN p_price DECIMAL(10,2)) :
--     가격을 바꾸되 p_price < 0 이면 SIGNAL 로 '가격은 음수가 될 수 없습니다' 에러.
--     존재하지 않는 상품이면 '상품 없음' 에러.
-- ---------------------------------------------------------------------
-- 여기에 작성
