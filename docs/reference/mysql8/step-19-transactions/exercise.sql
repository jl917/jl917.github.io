-- =====================================================================
-- Step 19 — 트랜잭션과 락  exercise.sql
-- ---------------------------------------------------------------------
-- 먼저 setup.sql 을 실행하세요.
-- [문제 3][문제 4][문제 5] 는 터미널 두 개가 필요합니다.
-- 공용 테이블은 절대 건드리지 말고 s19_* 사본만 쓰세요.
-- =====================================================================
USE shop;

-- ---------------------------------------------------------------------
-- [문제 1] 안전한 이체
--   account 4 (최영희, 잔액 0) 에서 account 1 로 10000원을 이체하려 합니다.
--   잔액이 부족하면 이체를 취소(ROLLBACK)해야 합니다.
--   트랜잭션을 열고, 출금 대상의 잔액을 확인한 뒤,
--   부족하면 ROLLBACK 하는 흐름을 작성하세요.
--   (힌트: 잔액이 0이므로 이 이체는 취소되어야 합니다)
-- ---------------------------------------------------------------------
-- 여기에 작성


-- ---------------------------------------------------------------------
-- [문제 2] SAVEPOINT
--   한 트랜잭션 안에서 account 1 에서 3000 출금 → SAVEPOINT →
--   account 2 에 5000 입금(실수로 많이 넣음) → 그 입금만 취소 →
--   account 2 에 3000 입금(올바르게) → COMMIT.
--   최종적으로 1은 -3000, 2는 +3000 이 되어야 합니다.
-- ---------------------------------------------------------------------
-- 여기에 작성


-- ---------------------------------------------------------------------
-- [문제 3] 재고 선점 (두 세션)
--   두 터미널에서 각각 seat 2 를 FOR UPDATE 로 잠그고 BOOKED 로 바꾸려 합니다.
--   A 가 먼저 잠근 뒤 커밋하지 않으면 B 는 어떻게 되나요?
--   실제로 실행해 보고, B 가 언제 깨어나는지 관찰하세요.
--   (session-a.sql / session-b.sql 의 [A-6]~[A-7], [B-5] 를 seat 2 로 바꿔 응용)
-- ---------------------------------------------------------------------
-- 여기에 작성 (관찰 결과를 주석으로)


-- ---------------------------------------------------------------------
-- [문제 4] 작업 큐 (두 세션)
--   두 워커(A, B)가 동시에 FREE 좌석을 하나씩 집으려 합니다.
--   SKIP LOCKED 를 써서 서로 다른 좌석을 잡도록 각 세션의 쿼리를 작성하세요.
--   (A 가 seat 1, B 가 seat 2 를 잡아야 함)
-- ---------------------------------------------------------------------
-- 여기에 작성


-- ---------------------------------------------------------------------
-- [문제 5] 데드락 방지 (두 세션)
--   session 예제는 A: 1→2, B: 2→1 순서로 잠가 데드락이 났습니다.
--   두 세션 모두 "account_id 오름차순"으로 잠그도록 바꾸면 데드락이 안 납니다.
--   B 가 잠그는 순서를 어떻게 바꿔야 하는지 쿼리로 쓰세요.
-- ---------------------------------------------------------------------
-- 여기에 작성


-- ---------------------------------------------------------------------
-- [문제 6] 결과 예측 (실행 없이 먼저 답을 쓰고, 그다음 확인)
--   A: SET SESSION isolation = ?; START TRANSACTION; SELECT balance ...(100000)
--   B: UPDATE ... +500; COMMIT;
--   A: SELECT balance ... 다시
--   격리수준이 (a) REPEATABLE READ 일 때와 (b) READ COMMITTED 일 때
--   A 의 두 번째 SELECT 결과를 각각 예측하세요.
-- ---------------------------------------------------------------------
-- 여기에 답 작성 (주석)
