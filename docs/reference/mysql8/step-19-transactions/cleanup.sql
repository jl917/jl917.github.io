-- =====================================================================
-- Step 19 — 트랜잭션과 락  cleanup.sql
-- ---------------------------------------------------------------------
-- 실습이 끝나면 실행하세요. 혹시 열린 트랜잭션이 있으면 먼저 롤백합니다.
-- =====================================================================
USE shop;
ROLLBACK;                 -- 열린 트랜잭션이 없으면 무해합니다
DROP TABLE IF EXISTS s19_accounts;
DROP TABLE IF EXISTS s19_seats;
SELECT 's19 cleanup 완료' AS msg;
