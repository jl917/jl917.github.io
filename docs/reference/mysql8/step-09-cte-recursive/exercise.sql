-- =====================================================================
-- Step 09 — CTE와 재귀 쿼리 : 연습문제
-- 실행: mysql -h127.0.0.1 -P3307 -ulearner -plearn1234 shop < exercise.sql
-- 정답은 solution.sql
-- =====================================================================
USE shop;

-- ---------------------------------------------------------------------
-- Q1. CTE 를 사용해 "카테고리별 상품 수"를 구하고,
--     상품이 4개 이상인 카테고리만 출력하시오.
--     컬럼: category_id, cat_name, product_cnt / 정렬: product_cnt DESC
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q2. 재귀 CTE 로 1~20 중 짝수만 출력하시오. (컬럼: n)
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q3. 재귀 CTE 로 employees 조직도를 전개하고,
--     각 사원의 depth(최상위=1)와 들여쓰기된 이름을 출력하시오.
--     컬럼: employee_id, depth, tree(들여쓰기+이름), position
--     정렬: 트리 순서(경로 문자열 기준)
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q4. employee_id = 17 (노총무) 의 모든 상위 관리자를 가까운 순서로 출력하시오.
--     컬럼: step(0=본인), employee_id, name, position
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q5. 2025-06-01 ~ 2025-06-15 의 일별 주문 건수를 출력하시오.
--     주문이 없는 날도 0 으로 반드시 나와야 한다. (15행)
--     컬럼: d, order_cnt
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q6. CTE 체이닝(2단 이상)으로
--     VIP/GOLD 등급 고객의 월별 매출(취소 제외)을 구하시오.
--     컬럼: ym, order_cnt, amount / 정렬: ym / 상위 6행만
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q7. 재귀 CTE 로 categories 계층을 전개하고
--     '대분류 / 소분류' 형태의 경로 문자열을 출력하시오.
--     컬럼: category_id, lvl, full_path / 정렬: full_path
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q8. cte_max_recursion_depth 를 세션 한정으로 조정해
--     1 ~ 3000 의 합을 재귀 CTE 로 구하시오. (검산: 4,501,500)
--     끝난 뒤 기본값 1000 으로 되돌릴 것.
-- ---------------------------------------------------------------------
