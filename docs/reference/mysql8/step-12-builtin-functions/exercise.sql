-- =====================================================================
-- Step 12 — 내장 함수 : 연습문제
-- 실행: mysql -h127.0.0.1 -P3307 -ulearner -plearn1234 shop < exercise.sql
-- 정답은 solution.sql
-- =====================================================================
USE shop;

-- ---------------------------------------------------------------------
-- Q1. 각 고객의 이메일에서
--     - 도메인(@ 뒤 전체)
--     - 로컬 파트(@ 앞)를 대문자로
--     를 출력하시오. 컬럼: email, domain, local_upper / 상위 5행
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q2. phone 이 있는 고객에 대해, 전화번호의 "뒤 4자리만" 남기고
--     나머지 숫자를 * 로 가린 값을 출력하시오. (REGEXP_REPLACE 사용)
--     예: 010-1000-0001 → ***-****-0001
--     컬럼: phone, masked / 상위 5행
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q3. 상품별 마진율(%)을 소수 1자리로,
--     ROUND 버전과 TRUNCATE 버전을 나란히 출력하시오.
--     컬럼: product_id, name, margin_round, margin_trunc / margin_round DESC 상위 5행
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q4. 각 주문의 "주문시각 ~ 결제시각" 소요 분(minute)을 구하고,
--     60분 이상 걸린 주문만 출력하시오.
--     컬럼: order_id, order_date, paid_at, minutes / minutes DESC 상위 5행
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q5. 고객을 나이대로 분류하시오. (birth_date 기준, CASE + TIMESTAMPDIFF)
--     20대 미만 / 20대 / 30대 / 40대 / 50대 이상
--     컬럼: name, age, age_band / age DESC 상위 8행
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q6. 취소 제외 주문의 월별 매출을, 라벨을 'YYYY년 MM월' 형식으로 집계하시오.
--     컬럼: ym_label, cnt, amount / 시간순 상위 6행
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q7. 모든 고객에 대해 phone 이 NULL 이면 '연락처없음' 으로 대체해 출력하시오.
--     (COALESCE 사용) 컬럼: customer_id, name, phone_or_default / phone 없는 고객만
-- ---------------------------------------------------------------------


-- ---------------------------------------------------------------------
-- Q8. "2025년 주문"을 조회하려 한다.
--     (a) 인덱스를 못 타는 나쁜 버전을 주석으로 쓰고
--     (b) 인덱스 친화적인 범위 조건 버전을 실제로 작성하시오.
--     각 버전에 EXPLAIN 을 붙여 type 컬럼을 비교하시오.
-- ---------------------------------------------------------------------
