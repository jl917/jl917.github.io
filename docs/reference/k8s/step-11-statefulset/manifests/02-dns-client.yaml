# DNS 확인용 클라이언트 파드 — nslookup 으로 파드별 안정 DNS 를 조회한다
apiVersion: v1
kind: Pod
metadata:
  name: dns-client
  namespace: step11
  labels:
    app: dns-client
spec:
  containers:
    - name: busybox
      image: busybox:1.36
      command: ["sleep", "3600"]
